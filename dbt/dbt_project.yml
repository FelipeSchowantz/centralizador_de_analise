name: "charles_river"
version: "1.0.0"
config-version: 2

profile: "charles_river"

model-paths:      ["models"]
test-paths:       ["tests"]
snapshot-paths:   ["snapshots"]
analysis-paths:   ["analyses"]
macro-paths:      ["macros"]
seed-paths:       ["seeds"]
target-path:      "target"
clean-targets:    ["target", "dbt_packages"]

models:
  charles_river:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
